using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemData : MonoBehaviour
{

    public uint item_id = 8008; // The unique id of the item
    public string name = "pork chop"; // the name of the item
    public string description = "some cooked pig"; // the desc. of the item

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
