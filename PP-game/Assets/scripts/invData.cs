using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class invData : MonoBehaviour
{

    public uint inv_id = 42;
    public string name = "backback";
    //public uint[] items;  -  An array of item ID - to be used in another portion of the code
    //public uint[] quantities; - An array of ints that represent the quantities of each item in the items array


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
