using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class playerData : MonoBehaviour { 

    public uint player_ID = 69420; // the unique id of the player
    public string username = "TheLegend27"; // the username of the player
    public string last_login = "12 hours ago"; // the last time the user logged in

        
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
